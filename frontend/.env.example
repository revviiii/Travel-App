EXPO_PUBLIC_SUPABASE_URL=http://10.0.2.2:54421
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_local_or_hosted_publishable_key
EXPO_PUBLIC_API_URL=http://10.0.2.2:8000

# Used only while creating a standalone Android build. Create a separate key,
# restrict it to Maps SDK for Android and this app's package/SHA-1, and store it
# in EAS rather than committing the value.
GOOGLE_MAPS_ANDROID_API_KEY=
